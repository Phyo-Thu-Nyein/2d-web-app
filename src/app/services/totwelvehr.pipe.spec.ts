import { TotwelvehrPipe } from './totwelvehr.pipe';

describe('TotwelvehrPipe', () => {
  it('create an instance', () => {
    const pipe = new TotwelvehrPipe();
    expect(pipe).toBeTruthy();
  });
});
